services:
  api:
    build:
      context: .
    env_file:
      - .env
    environment:
      LLM_BASE_URL: "http://host.docker.internal:8001"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"
    restart: unless-stopped
