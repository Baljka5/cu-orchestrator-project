services:
  api:
    build:
      context: .
    env_file:
      - .env
    environment:
      LLM_BASE_URL: "http://host.docker.internal:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8001:8000"
    restart: unless-stopped
